services:
  builder:
    container_name: khoa-portfolio-builder
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "8080:5173"
    volumes:
      - ./:/app
      - /app/node_modules